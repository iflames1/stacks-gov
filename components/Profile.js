import React from "react";

export default function Profile() {
  return (
    <div>
      User’s voting history, List of proposals created by the user, Personal
      statistics (e.g., number of votes participated in, proposals created)
    </div>
  );
}
