import React from "react";

export default function Setting() {
  return (
    <div>
      User preferences and settings Notification settings (e.g., email/console
      notifications for new proposals, voting reminders)
    </div>
  );
}
